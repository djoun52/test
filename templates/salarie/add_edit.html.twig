{% extends 'base.html.twig' %}

{% block title %}Liste des salariés{% endblock %}

{% block body %}

    <a href="{{ path('salarie_index') }}">Retour à la liste des salariés</a>

    {% if editMode %}
        <h1>Modification d'un salarié</h1>
    {% else %}
        <h1>Ajout d'un salarié</h1>
    {% endif %}
    
    {{ form(formSalarie) }}

{% endblock %}
